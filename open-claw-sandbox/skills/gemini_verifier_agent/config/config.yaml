# config/config.yaml for gemini_verifier_agent
model_profile: default
models:
  default:
    name: "qwen3:8b"
    temperature: 0.0

directories:
  input: "data/gemini_verifier_agent/input"
  output: "data/gemini_verifier_agent/output"
  state: "data/gemini_verifier_agent/state"

phases:
  p01_ai_debate:
    enabled: true
    timeout_seconds: 120
    gemini_url: "https://gemini.google.com"
