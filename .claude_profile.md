# Operator Profile — Jinkun

## Identity
- **Role**: Operator & primary developer of the `local-workspace` monorepo
- **Working style**: Direct, precise, production-grade output only — no scaffolding, no stubs
- **Hardware**: macOS Apple Silicon, 16 GB RAM

## Preferences
- All code and documentation in English
- Commit messages in Conventional Commits format
- One focused change per commit — no "mega commits"
- Always update `memory/HANDOFF.md` at end of session

## What AI Should NOT Do
- Do not produce partial implementations without marking `# TODO(reason)`
- Do not invent paths or config keys — always `ls` or `cat` first
- Do not modify files outside `open-claw-sandbox/` without explicit approval
- Do not start.sh / stop.sh while services may be running without asking first

## Workspace Entry Point
- Monorepo root: `local-workspace/`
- Sandbox: `local-workspace/open-claw-sandbox/`
- Read first: `memory/ARCHITECTURE.md` → `open-claw-sandbox/memory/CLAUDE.md` → `open-claw-sandbox/AGENTS.md`